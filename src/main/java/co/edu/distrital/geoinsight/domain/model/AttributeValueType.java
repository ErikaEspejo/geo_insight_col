package co.edu.distrital.geoinsight.domain.model;

/** Tipo escalar observado para un atributo en el dataset de origen. */
public enum AttributeValueType {
    TEXT,
    INTEGER,
    DECIMAL,
    BOOLEAN
}
